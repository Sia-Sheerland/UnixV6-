# Unix V6++ 离散化内存管理实现报告

---

## 一、背景：原有系统的问题

原系统采用**静态连续分配**模型。进程 exec 时，text、data、stack 三个段作为一个整体，通过 `AllocMemory(p_size)` 一次性申请一块**物理上连续**的内存，再用 `EstablishUserPageTable()` 把这块连续内存整体映射进页表。fork 时同样用 `CopySeg` 把整块内存逐页复制给子进程。

这种方式有两个根本缺陷：

**第一，物理内存必须连续**。随着进程数增多，内存碎片化严重，即使总空闲内存足够，也可能找不到一块足够大的连续区域分配给新进程。

**第二，无法独立管理每一页**。所有页打包在一起，无法知道某一页被几个进程共享，也无法单独释放某一页，更无法实现后续的写时复制等机制。

离散化的目标就是打破这两点限制：**每一页独立分配，物理上可以不连续，每一页单独跟踪生命周期**。

---

## 二、离散化的核心改动

### 2.1 Shadow 页表承担离散映射职责

原来 Shadow 页表只是在进程切换时整体复制到硬件，内容在进程运行期间基本不变。

离散化之后，Shadow 页表成为**动态维护的核心结构**。每分配一个物理页，就在 Shadow 里建立一条独立的映射记录；每释放一个物理页，就清除对应的表项。Shadow 页表不再是静态快照，而是进程当前内存状态的实时描述。

为此新增了 `MapPageDirect()` 接口，实现单页粒度的映射操作：

```cpp
void MemoryDescriptor::MapPageDirect(unsigned long va,
                                     unsigned long physFrame,
                                     bool writable)
{
    // 更新 shadow 页表
    PageTableEntry& spte = GetPTE(va);
    spte.m_Present         = 1;
    spte.m_ReadWriter      = writable ? 1 : 0;
    spte.m_PageBaseAddress = physFrame;

    // 同步更新硬件页表
    PageTable* hwPT = Machine::Instance().GetUserPageTableArray();
    unsigned int pageNum  = va >> 12;
    unsigned int tableIdx = pageNum >> 10;
    unsigned int entryIdx = pageNum & 0x3FF;
    hwPT[tableIdx].m_Entrys[entryIdx] = spte;
    FlushPageDirectory();
}
```

对应地，`UnmapPage()` 实现单页解除映射，`GetPTE()` 通过虚拟地址直接定位到对应的页表项。

---

### 2.2 Exec 改为逐页分配

原来 exec 调用一次 `AllocMemory(textSize + dataSize + stackSize)` 申请整块连续内存。

改造后，text、data、stack 各自按页独立分配：

```
text 段：
  for 每一页 p:
      physPage = userPgMgr.AllocMemory(PAGE_SIZE)
      MapPageDirect(textVA + p*PAGE_SIZE, physPage >> 12, false)

data 段：
  for 每一页 p:
      physPage = userPgMgr.AllocMemory(PAGE_SIZE)
      PageRefCount::Set(physPage, 1)
      MapPageDirect(dataVA + p*PAGE_SIZE, physPage >> 12, true)

stack 段：同 data
```

每次 `AllocMemory(PAGE_SIZE)` 只申请一个物理页，这个物理页可以来自内存中任意位置，不要求与上一页相邻。多个页分配完之后，通过 Shadow 页表里各自独立的映射关系，在虚拟地址空间上呈现为连续的段，但物理上完全离散。

---

### 2.3 PageRefCount：每页独立的引用计数

连续分配模型下，一块内存作为整体分配、整体释放，不需要知道其中某一页被几个人用。

离散化之后，不同进程可能共享同一个物理页（例如 fork 后的 COW 共享），需要知道每一页具体的引用情况，才能判断什么时候可以安全释放。

为此引入 `PageRefCount`：

```cpp
static unsigned char s_counts[8192];  // 下标 = 物理帧号 - 1024
```

- `Set(physAddr, 1)`：页首次分配时初始化引用计数为 1
- `Inc(physAddr)`：新增一个引用（fork 共享时）
- `Dec(physAddr)`：减少一个引用
- `Get(physAddr)`：查询当前引用数，为 0 时释放

---

### 2.4 FreeUserPages：逐页释放

原来释放进程内存是 `FreeMemory(p_size, p_addr)`，一次释放整块。

离散化之后，每一页需要单独处理：

```
for 页表中每一个 Present 的表项:
    frm = 该表项的物理帧号
    if frm 在 text 段范围内:
        跳过（text 由 Text 结构统一管理）
    else:
        PageRefCount::Dec(frm << 12)
        if PageRefCount::Get(frm << 12) == 0:
            FreeMemory(PAGE_SIZE, frm << 12)
        清除该页表项
```

这样每一页根据自己的引用计数独立决定是否释放，不同页可以有不同的生命周期。

---

### 2.5 fork 改为逐页共享

原来 fork 复制整块内存。离散化之后，fork 遍历父进程 Shadow 页表，对每一个存在的页表项：

```
for 父进程 shadow 中每个 Present 的页:
    子进程 shadow[同一位置] = 父进程 shadow[同一位置]  // 共享同一物理帧
    PageRefCount::Inc(该物理帧)                        // 引用计数 +1
```

子进程和父进程的页表项指向相同的物理帧，引用计数反映共享状态。后续谁需要独立修改，再单独分配新页，更新自己的页表项，引用计数相应调整。

---

## 三、离散化前后对比

| 项目 | 原来（连续） | 现在（离散） |
|------|-------------|-------------|
| 内存分配单位 | 整个进程（text+data+stack） | 单页（4KB） |
| 物理连续性要求 | 必须连续 | 不要求 |
| fork 内存操作 | 逐页复制整块 | 共享页表项，引用计数 +1 |
| 单页释放 | 不支持 | FreeUserPages 逐页 Dec 引用计数 |
| 每页引用追踪 | 无 | PageRefCount 数组 |
| 页表动态更新 | 静态，进程运行期间不变 | 动态，MapPageDirect 随时更新单页 |

---

## 四、总结

离散化的本质是把内存管理的粒度从"进程整块"缩小到"单个物理页"。每一页独立分配、独立映射、独立追踪引用、独立释放。Shadow 页表负责记录每一页的映射关系，PageRefCount 负责记录每一页的共享状态，FreeUserPages 负责按页回收，MapPageDirect 负责按页建立映射。

这四个机制共同构成离散化的基础，在此之上，按需调页、写时复制等更高层的功能才有了实现的条件。
