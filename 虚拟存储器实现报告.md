# Unix V6++ 按需调页式虚拟存储器实现报告

---

## 第一部分：虚拟存储器设计与实现

### 1.1 设计目标

将原有的**静态连续内存分配**改造为**离散化按需调页（Demand Paging）**虚拟存储器，具体目标：

- **按需分配**：进程运行时不预先分配全部内存，只在缺页时才分配物理页
- **写时复制（COW）**：fork 后父子进程共享物理页，写操作触发复制，避免无谓的内存拷贝
- **引用计数**：多进程共享同一物理页时用引用计数管理，最后一个引用释放时才回收物理内存
- **VMA 描述符**：用虚拟内存区域链表描述进程的地址空间布局，支持不同区域不同权限

---

### 1.2 核心数据结构

#### 1.2.1 Shadow 页表（Per-Process Shadow Page Table）

**文件**：`MemoryDescriptor.h / MemoryDescriptor.cpp`

```
硬件页表（Machine::GetUserPageTableArray()）
  └─ 唯一一份，进程切换时从 shadow 整体覆盖
  
Shadow 页表（MemoryDescriptor::m_UserPageTableArray）
  └─ 每进程一份，存于内核堆（KernelPageManager 分配）
  └─ 进程切换时：MapToPageTable() 把 shadow 复制到硬件，FlushPageDirectory() 刷 TLB
```

- 覆盖用户地址空间 0x00000000 ~ 0x00800000（8MB），2 个 PageTable 结构
- 每个 PageTableEntry 包含：Present、ReadWriter、UserSupervisor、PageBaseAddress 等字段
- `MapPageDirect(va, frame, writable)`：同时更新 shadow 和硬件页表，并刷新 TLB
- `UnmapPage(va)`：同时清除 shadow 和硬件页表

#### 1.2.2 VMA 链表（Virtual Memory Area）

**文件**：`VmAreaStruct.h / VmAreaStruct.cpp`

每个进程维护一条 VMA 链表，描述各虚拟地址区域：

| 区域 | vm_flags | vm_prot | 说明 |
|------|----------|---------|------|
| text | VM_TEXT | R/X | 可执行文件代码段，文件支撑 |
| data | VM_DATA | R/W | 已初始化数据段，文件支撑 |
| bss  | VM_BSS\|VM_ANON | R/W | 未初始化数据，匿名（清零） |
| heap | VM_HEAP\|VM_ANON | R/W | 堆区，初始大小为 0 |
| stack | VM_STACK\|VM_ANON | R/W | 用户栈，向下增长 |

VMA 通过静态池管理（`g_VmaPool[256]`），`VmaAlloc()` / `VmaFree()` 分配释放。
fork 时通过 `CloneVmaList()` 深拷贝一份给子进程。

#### 1.2.3 物理页引用计数（PageRefCount）

**文件**：`PageRefCount.h / PageRefCount.cpp`

```cpp
static unsigned char s_counts[8192];  // 下标 = 物理帧号 - 1024
```

- 用户页从物理地址 0x400000 开始（帧号 1024），s_counts[0] 对应帧 1024
- `Set(physAddr, n)`：初始化引用计数（exec 分配 data/stack 时设为 1）
- `Inc(physAddr)`：引用计数 +1（fork COW 时对所有共享页调用）
- `Dec(physAddr)`：引用计数 -1，减到 0 时调用者负责释放物理页
- `Get(physAddr)`：读取当前引用计数

#### 1.2.4 Text 结构（代码段共享）

**文件**：`Text.h / Text.cpp`

```cpp
struct Text {
    Inode*         x_iptr;   // 对应可执行文件的 inode
    unsigned long  x_caddr;  // 代码段物理内存起始地址
    unsigned long  x_daddr;  // 代码段在 Swap 中的起始块号
    int            x_size;   // 代码段大小（字节）
    int            x_count;  // 引用该代码段的进程数（含换出）
    int            x_ccount; // 引用该代码段内存拷贝的进程数
};
```

- `x_count / x_ccount` 分别管理 swap 引用和内存引用
- 多个进程执行同一可执行文件时共享同一 Text 结构，物理 text 页只有一份
- `XFree()` / `XccDec()`：减少引用计数，归零时分别释放 swap 块 / 物理内存

---

### 1.3 缺页处理（Demand Paging Handler）

**文件**：`Exception.cpp` — `Exception::PageFault()`

缺页中断（INT 14）触发时，读取 CR2 寄存器获取出错虚拟地址，按三种情形处理：

```
┌─────────────────────────────────────────────────────────────────┐
│                        缺页处理流程                              │
│                                                                   │
│  读 CR2 → 查 VMA → 权限检查                                      │
│                    │                                              │
│           ┌────────┴────────┐                                    │
│     无 VMA？              有 VMA                                  │
│      是否栈扩展？               │                                 │
│                    ┌───────────┴──────────────┐                  │
│              情形 A              情形 B          情形 C           │
│          P=0,frame=0         P=0,frame≠0      P=1,R/O,写操作    │
│          (首次访问)          (已换出 Swap)      (写时复制)        │
│               │                   │                  │           │
│         分配新物理页         从 Swap 读回       refcount > 1?    │
│         Set refcount=1       Set refcount=1    ├─ 是：分配新页  │
│         建立页表映射          建立页表映射      │   复制旧页内容  │
│         文件支撑则读文件      释放 Swap 块      │   Dec 旧引用   │
│         匿名则清零                              └─ 否：直接改R/W │
└─────────────────────────────────────────────────────────────────┘
```

**情形 A（首次访问）** — 最常见：

```cpp
unsigned long physPage = upm.AllocMemory(PAGE_SIZE);
PageRefCount::Set(physPage, 1);
md.MapPageDirect(pageVA, physPage >> 12, writable);
// 文件支撑 VMA：从 inode 读入对应偏移的数据
// 匿名 VMA：清零
```

**情形 B（Swap 换入）**：

```cpp
unsigned long physPage = upm.AllocMemory(PAGE_SIZE);
PageRefCount::Set(physPage, 1);
md.MapPageDirect(pageVA, physPage >> 12, writable);
bufMgr.Swap(blkno, physPage, PAGE_SIZE, B_READ);
swapperMgr.FreeSwap(PAGE_SIZE, blkno);
```

**情形 C（COW 写时复制）**：

```cpp
unsigned char refCnt = PageRefCount::Get(oldPhys);
if (refCnt > 1) {
    // 借用内核页表两个槽位做物理页间复制
    unsigned long newPage = upm.AllocMemory(PAGE_SIZE);
    PageRefCount::Set(newPage, 1);
    PageRefCount::Dec(oldPhys);
    // kpt[258] 映射旧页，kpt[259] 映射新页，memcpy
    md.MapPageDirect(pageVA, newPage >> 12, true);
} else {
    // 唯一所有者，直接改写权限
    md.MapPageDirect(pageVA, spte.m_PageBaseAddress, true);
}
```

---

### 1.4 Fork 实现（COW Fork）

**文件**：`ProcessManager.cpp` — `ProcessManager::NewProc()`

```
原来 fork：整块物理内存拷贝（几 MB），O(n) 时间和空间
现在 fork：只复制页表（8KB shadow），O(1)，物理页写时才拷贝
```

**流程**：

```
1. Clone() 复制 Process 结构
   └─ 同时：x_count++, x_ccount++（防止子进程 exec 误释放父进程 text）

2. SaveU(u.u_rsav)  ← 保存父进程的 ESP/EBP（子进程恢复时从这里继续执行）

3. 分配子进程 shadow 页表（KernelPageManager）

4. 遍历父进程 shadow 页表：
   └─ 每个 Present 的页：
       ├─ 若 R/W：父子两边都改为 R/O（COW 保护）
       └─ PageRefCount::Inc(physAddr)（增加共享引用）
   └─ 更新父进程硬件页表（MapToPageTable）

5. CloneVmaList() 深拷贝 VMA 链表给子进程

6. u.u_procp = child，AllocMemory(USIZE) 分配子进程 ppda
   └─ CopySeg 把父进程 ppda 整页复制到子进程 ppda

7. SwtchUStruct(child)：
   └─ 向子进程 ppda 写入：shadow 页表指针、VMA 链表、u_procp=child
   └─ SwtchUStruct(current)：切回父进程 ppda

8. 恢复父进程 u_procp 和 m_UserPageTableArray

9. return 0（父进程路径）

子进程被调度时（Swtch → RetU）：
   └─ 恢复父进程保存的 ESP/EBP，u.u_procp == child → return 1
   └─ Fork() 看到 NewProc() 返回 1 → 设置 u_ar0[EAX]=0（fork 返回子进程 pid=0）
```

---

### 1.5 Exec 实现

**文件**：`ProcessManager.cpp` — `ProcessManager::Exec()`

```
原来：一次性 AllocMemory(text+data+stack)，连续分配
现在：text 共享，data/stack 逐页分配并设引用计数，其余按需
```

**关键步骤**：

```
1. FreeUserPages(u)    ← 释放旧进程 data/stack 物理页（跳过 text）
2. FreeAllVmas()       ← 释放旧 VMA 链表
3. XFree(old text)     ← 减少旧代码段引用

4. 查找/创建 Text 结构（同一程序可共享）
   ├─ 已存在：x_count++, x_ccount++，共享物理 text 页
   └─ 新建：AllocMemory(text_size)，从文件加载，写入 Swap 备份，改为 R/O

5. 逐页分配 data 段：AllocMemory(PAGE_SIZE) × n
   └─ PageRefCount::Set(page, 1)，MapPageDirect

6. 逐页分配 stack 段：同上

7. MapToPageTable() 同步到硬件

8. Relocate() 加载各节内容

9. 建立新 VMA 链表（text/data/bss/heap/stack）

其余页（bss/heap 扩展/栈扩展）：P=0，等缺页时按需分配
```

---

### 1.6 Exit 实现

**文件**：`Process.cpp` — `Process::Exit()`

**正确的释放顺序**（关键！）：

```
1. FreeUserPages(u)    ← p_textp 此时仍有效，正确跳过 text 页框
                          data/stack 页：Dec refcount，归零则 FreeMemory
2. FreeAllVmas()       ← 释放 VMA 链表，IPut inode 引用
3. 保存 u 到 Swap（供 Wait() 读取 exit status 和时间统计）
4. XFree(p_textp)      ← 减少 text 引用，x_ccount 归零时 XccDec 释放物理 text 页
5. Release()           ← 释放 shadow 页表的内核内存
6. FreeMemory(p_addr)  ← 释放 ppda（进程 User 结构所在页）
7. p_stat = SZOMB      ← 进入僵尸态，等父进程 Wait() 回收
```

---

## 第二部分：Bug 清单与修复记录

### Bug 1：热重启后 "STDIN Error!"

**现象**：Bochs 热重启（warm reboot）后，Shell 无法读取键盘输入，显示 "STDIN Error!"。

**根因**：物理地址 0x3FF000 存放进程 0 的 ppda（User 结构），其中包含 `u_ofiles`（进程打开文件表）。热重启后 RAM 内容保留，残留上次会话的文件描述符指针，`AllocFreeSlot()` 认为 fd=0 已被占用，返回 fd=1，导致 stdin 绑定错误。

**修复**：在 `SetupProcessZero()` 中明确清零文件表：

```cpp
// ProcessManager.cpp — SetupProcessZero()
for (int i = 0; i < OpenFiles::NOFILES; i++)
    u.u_ofiles.SetF(i, NULL);
```

**文件**：`ProcessManager.cpp`

---

### Bug 2：COW 写操作触发无限缺页循环 → Triple Fault → 重启

**现象**：fork 后父进程写数据页，系统进入无限缺页中断循环，最终三重故障重启。

**根因**：`MapPageDirect()` 在 COW 情形 C 分配新页并更新 shadow 页表、硬件页表后，**没有刷新 TLB**。CPU TLB 中缓存了旧的 R/O 表项，下一条写指令仍然命中旧的 R/O 映射，再次触发缺页，无限循环。

**修复**：在 `MapPageDirect()` 末尾加 `FlushPageDirectory()`：

```cpp
// MemoryDescriptor.cpp — MapPageDirect()
PageTable* hwPT = Machine::Instance().GetUserPageTableArray();
hwPT[tableIdx].m_Entrys[entryIdx] = spte;
FlushPageDirectory();   // ← 新增，刷新 TLB
```

**文件**：`MemoryDescriptor.cpp`

---

### Bug 3：第二条命令无响应，第三条命令系统卡死

**现象**：Shell 输入第一条命令正常执行，第二条无响应，第三条系统完全卡死。

**根因（双重 Bug，需同时修复）**：

#### Bug 3a：子进程 exec() 释放父进程 text 物理页

`Clone()` 把父进程的 `p_textp`（text 结构指针）复制给子进程，但**没有递增 `x_count` 和 `x_ccount`**。

子进程调用 `Exec()` 时执行 `XFree()`：
- `x_ccount`：1 → 0 → `XccDec()` 调用 `FreeMemory()` **释放 text 物理页**
- `x_count`：1 → 0 → `IPut()` 释放 inode

此时父进程 Shell **仍在执行这些已被释放的 text 页**，内存被重新分配给其他用途后，Shell 代码段被覆盖。

**修复**：在 `NewProc()` 中 `Clone()` 之后立即增加 text 引用计数：

```cpp
// ProcessManager.cpp — NewProc()
current->Clone(*child);
if (child->p_textp) {
    child->p_textp->x_count++;    // 子进程持有一份引用
    child->p_textp->x_ccount++;
}
```

**文件**：`ProcessManager.cpp`

#### Bug 3b：Exit() 中 XFree 先于 FreeUserPages 导致 text 页双重释放

`Process::Exit()` 的原始顺序是：
```
XFree(p_textp)  →  p_textp = NULL  →  FreeUserPages()
```

`XFree()` 将 `p_textp` 置为 NULL，随后 `FreeUserPages()` 无法获得 text 帧范围来跳过它们，于是遍历 shadow 页表时找到 text 帧，将其 `PageRefCount::Dec()` 减到 0，**再次调用 `FreeMemory()` 释放已被 `XccDec` 释放过的 text 物理页**。

这导致 `UserPageManager` 的空闲页池被破坏（同一帧被释放两次），后续分配返回错误的帧，内存随机损坏。

**修复**：调换顺序，`FreeUserPages()` 必须先于 `XFree()` 执行：

```cpp
// Process.cpp — Exit()（修复后顺序）

// ① 先释放 data/stack 物理页（p_textp 有效，text 帧被正确跳过）
ProcessManager::FreeUserPages(u);
u.u_MemoryDescriptor.FreeAllVmas();

// ② 保存 u 到 Swap
...

// ③ 再减 text 引用计数（此时才可以 x_ccount--，必要时释放 text 物理页）
if (u.u_procp->p_textp != NULL) {
    u.u_procp->p_textp->XFree();
    u.u_procp->p_textp = NULL;
}

// ④ 释放 shadow 页表内核内存
u.u_MemoryDescriptor.Release();
```

**文件**：`Process.cpp`

---

## 第三部分：各模块修改汇总

| 文件 | 修改内容 |
|------|----------|
| `ProcessManager.cpp` | `SetupProcessZero()`：清零文件表防热重启残留 |
| `ProcessManager.cpp` | `NewProc()`：COW fork 实现；Clone 后递增 text 引用计数 |
| `ProcessManager.cpp` | `Exec()`：逐页分配 data/stack，建立 VMA，text 共享 |
| `ProcessManager.cpp` | `FreeUserPages()`：新增，按引用计数逐页释放，跳过 text |
| `Process.cpp` | `Exit()`：调整释放顺序：FreeUserPages → XFree → Release |
| `MemoryDescriptor.cpp` | `MapPageDirect()`：更新硬件页表后加 FlushPageDirectory |
| `MemoryDescriptor.cpp` | `EstablishUserPageTable()`：只映射 text，其余留给缺页 |
| `Exception.cpp` | `PageFault()`：实现情形 A/B/C 完整缺页处理逻辑 |
| `VmAreaStruct.cpp` | 新增：静态 VMA 池，VmaAlloc/VmaFree/VmaCloneList |
| `PageRefCount.cpp` | 新增：物理页引用计数数组，Inc/Dec/Set/Get |

---

## 第四部分：运行效果验证

### 验证项目

1. **按需调页**：程序启动时只分配少量页，随着执行触发缺页（type=NEW），内存占用逐步增长
2. **COW 验证**：fork 后父子进程各自修改变量，值互不影响（COW 日志显示 type=COW）
3. **多命令连续执行**：Shell 可以连续执行多条命令（date/ls/cat 等）而不崩溃
4. **内存无泄漏**：多条命令执行前后空闲页数基本不变，说明每个进程退出后内存被正确回收

---

*实现基于 Unix V6++ 教学操作系统，运行于 Bochs x86 模拟器，i386 保护模式，32 位单级用户页表结构。*
